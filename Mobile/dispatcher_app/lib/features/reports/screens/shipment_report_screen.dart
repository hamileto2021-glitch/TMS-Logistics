import 'package:flutter/material.dart';

class ShipmentReportScreen extends StatelessWidget {
  const ShipmentReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Shipment Report"),
      ),
      body: const Center(
        child: Text("Coming Soon"),
      ),
    );
  }
}